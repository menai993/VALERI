"""Capability self-configuration (CSA Phase 3): self-proposed metrics.

A proposal is drafted and stored inert; a human approves it (reversible,
logged), after which it joins the registry overlay and becomes a real,
validated metric. The LLM drafts; the safety validator + human gate.
"""
